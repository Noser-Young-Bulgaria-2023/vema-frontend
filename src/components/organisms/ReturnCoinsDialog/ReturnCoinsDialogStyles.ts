import { SxProps } from "@mui/material";

const box: SxProps = {
  padding: 2,
};

const gridItem: SxProps = {
  display: "flex",
  flexDirection: "column",
  alignItem: "center",
};

const coinCountText: SxProps = {
  textAlign: "center",
};

export default { box, gridItem, coinCountText };
