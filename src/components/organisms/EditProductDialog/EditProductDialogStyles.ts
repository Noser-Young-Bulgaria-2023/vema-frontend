import { SxProps } from "@mui/material";

const productImage: SxProps = {
  fontSize: "5rem",
};

const productImagePreview: SxProps = {
  maxHeight: "150px",
};

const productImagePreviewSection: SxProps = {
  display: "flex",
  flexDirection: "column",
  alignItems: "center",
};

export default {
  productImage,
  productImagePreview,
  productImagePreviewSection,
};
