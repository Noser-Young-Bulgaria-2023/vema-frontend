import { SxProps } from "@mui/material";

const page: SxProps = {
  maxWidth: "1920px",
  display: "flex",
  flexDirection: "column",
  alignItems: "center",
};

const logo: SxProps = {
    margin: "12",
};

export default { page, logo };
